{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://syntara.io/schemas/manifest.schema.json",
  "title": "Syntara Node Manifest Schema",
  "description": "Top-level validation schema for node manifest.yaml files. Validates the Kubernetes CRD structure (apiVersion, kind, metadata, spec) against the canonical NodeTypeManifest definition.",
  "$ref": "schemas/common-definitions.json#/definitions/NodeTypeManifest"
}
